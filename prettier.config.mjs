/** @type {import("prettier").Config} */
const config = {
  plugins: [],
  printWidth: 100,
  proseWrap: "preserve",
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default config;
